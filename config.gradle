def majorVersion = 0
def minorVersion = 1
def patchVersion = 3

def version_code = majorVersion * 1000 + minorVersion * 10 + patchVersion

def version_name = majorVersion + "." + minorVersion + "." + patchVersion
ext{
    version=version_name
}